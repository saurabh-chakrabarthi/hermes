# DEPRECATED - This Dockerfile is no longer used
# 
# Build the entire project from the repository root using:
#   docker build -f payment-infra/Dockerfile -t payment-dashboard .
#
# The consolidated Dockerfile in payment-infra/ handles the complete Maven build
# with proper parent POM resolution and module compilation.
